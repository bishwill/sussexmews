name: deploy

on:
  push:
    branches:
      - gh-action-runner
      - main

jobs:

  deploy-website:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: deploy
        run: docker run hello-world
